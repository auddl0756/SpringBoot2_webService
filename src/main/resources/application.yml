spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5InnoDBDialect
  h2:
    console:
      enabled: true

  transaction:
    default-timeout: 10

  datasource:
    hikari:
      connection-timeout: 10000
      maximum-pool-size: 30

